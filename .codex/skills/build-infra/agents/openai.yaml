interface:
  display_name: "Build Infra"
  short_description: "Build queued data-source infrastructure"
  default_prompt: "Use $build-infra to handle this investigation task with clear evidence and actionable output."
