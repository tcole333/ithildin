interface:
  display_name: "Build Infra"
  short_description: "Build queued data-source infrastructure"
  default_prompt: "Use $build-infra to implement the selected infrastructure request, or scan source gaps when requested, with verified access and isolated acceptance tests."
