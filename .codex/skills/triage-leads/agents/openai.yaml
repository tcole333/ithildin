"interface":
  "display_name": "Triage Leads"
  "short_description": "Review pending leads with scoped, atomic decisions"
  "default_prompt": "Use $triage-leads. Review a database-bound pending-lead packet,\
    \ preserve distinct questions, and apply justified decisions only against current\
    \ matching state."
