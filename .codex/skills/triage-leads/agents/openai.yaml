interface:
  display_name: "Triage Leads"
  short_description: "Deduplicate, prioritize, and route new leads"
  default_prompt: "Use $triage-leads to handle this investigation task with clear evidence and actionable output."
