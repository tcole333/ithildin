interface:
  display_name: "Deduplicate Leads"
  short_description: "Review and consolidate duplicate leads"
  default_prompt: "Use $dedup-leads to handle this investigation task with clear evidence and actionable output."
