interface:
  display_name: "Generate Hunches"
  short_description: "Help with Generate Hunches tasks"
  default_prompt: "Use $generate-hunches to handle this task and return concise, actionable output."
