interface:
  display_name: "Generate Hunches"
  short_description: "Find emerging patterns across findings"
  default_prompt: "Use $generate-hunches to handle this investigation task with clear evidence and actionable output."
