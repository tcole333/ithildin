interface:
  display_name: "Add Registry"
  short_description: "Add a corporate registry data source"
  default_prompt: "Use $add-registry to build a verified jurisdiction adapter that preserves entity identity and filing history across repeated ingestion."
