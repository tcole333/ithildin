interface:
  display_name: "Dispatch"
  short_description: "Report queue depth and recommend next work"
  default_prompt: "Use $dispatch to handle this investigation task with clear evidence and actionable output."
