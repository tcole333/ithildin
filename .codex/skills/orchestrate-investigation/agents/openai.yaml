interface:
  display_name: "Orchestrate Investigation"
  short_description: "Control staged investigation worker runs"
  default_prompt: "Use $orchestrate-investigation to handle this investigation task with clear evidence and actionable output."
