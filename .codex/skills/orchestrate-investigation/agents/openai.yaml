interface:
  display_name: "Orchestrate Investigation"
  short_description: "Run the staged investigation control plane"
  default_prompt: "Use $orchestrate-investigation to plan, launch, review, and import investigation workers through the shared dispatcher."
