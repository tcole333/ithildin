interface:
  display_name: "Initialize Investigation"
  short_description: "Create and seed an investigation profile"
  default_prompt: "Use $init-investigation to handle this investigation task with clear evidence and actionable output."
