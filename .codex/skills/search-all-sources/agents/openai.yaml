"interface":
  "display_name": "Search All Sources"
  "short_description": "Find evidence across applicable local and remote data"
  "default_prompt": "Use $search-all-sources. Look up the requested term across relevant\
    \ available sources, reuse only valid result artifacts, and return evidence with\
    \ explicit coverage."
