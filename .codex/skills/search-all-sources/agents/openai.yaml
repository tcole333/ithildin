interface:
  display_name: "Search All Sources"
  short_description: "Search relevant local and remote data sources"
  default_prompt: "Use $search-all-sources to handle this investigation task with clear evidence and actionable output."
