interface:
  display_name: "Trace Grants"
  short_description: "Map nonprofit grants and funding networks"
  default_prompt: "Use $trace-grants to trace the requested nonprofit funding question, reproduce material flows from filing rows, and report the observed graph's coverage."
