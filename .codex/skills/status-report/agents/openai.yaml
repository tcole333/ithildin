interface:
  display_name: "Status Report"
  short_description: "Summarize leads, findings, and source coverage"
  default_prompt: "Use $status-report to handle this investigation task with clear evidence and actionable output."
