interface:
  display_name: "Landscape Scan"
  short_description: "Rapidly map targets, leads, and relationships"
  default_prompt: "Use $landscape-scan to handle this investigation task with clear evidence and actionable output."
