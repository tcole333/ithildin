# Platform hardening and repository cleanup

Requested outcome: implement the September 2026 platform review, clean up the repository while preserving existing work, and establish an enforceable Git branching/commit policy.

Review: `reports/platform-review-2026-09-05/review.md` and its six component reports. This ledger maps the 46 reviewed requirements to their implementation and verification evidence. Focused test counts overlap and must not be added together.

Baseline: commit `b9a15d2e5ae72b25087bbf5ef4c9d9a0a044a683`, formerly on `codex/post-publish-source-and-karp-20260715`. Before implementation, 4,867 modified/untracked files were archived with per-file hashes at the ignored local path `backups/platform-hardening-20260905-baseline/`. The implementation branch is `codex/platform-hardening-20260905`. No remote publication/push is part of this request.

| ID | Requirement | State | Completion evidence |
|---|---|---|---|
| D1 | One transaction-aware finding writer shared by normal and staged imports | Implemented | `findings_tracker.add_finding_to_db` is transaction-owned and shared by staged import; `test_finding_mutation_invariants.py` and `test_orchestration_hardening.py` cover quoted refs, normalized dates, links, rollback and replay. `implemented-strict-finding-inserts.md`: 320 combined tests including producers. |
| D2 | Claim correction invalidates verification and reconciles subject/date links | Implemented | Mutation regressions cover semantic correction invalidation, same-value preservation, canonical subject reconciliation, ambiguous-target rollback; date corrections use canonical audited API. |
| D3 | Confidence/provenance caps apply on add, correction, and verification | Implemented | Shared candidate caps on add/correct/verify and upstream connection publication; explicit confidence-correction and preexisting over-cap verification rejection tests. Historical above-cap rows were not silently repaired. Snapshot integration adds static cap checks. |
| D4 | Alias identity checks respect type, jurisdiction, and strong identifiers | Implemented | `tests/test_entity_resolution_identity_guards.py`: alias/exact conflicts for jurisdiction/EIN/type, punctuation-equivalent compatible EIN and organization/person separation. |
| D5 | Versioned bootstrap and historical migrations preserve provenance/FKs | Implemented | `implemented-data-schema.md`: 12 bootstrap/historical/FK tests; shared schema ledger, additive DDL, preserved provenance/generated columns/indexes/triggers/sequence/FK setting and rollback. Final ingestion handoff adds six per-database/replacement/schema-cache regressions and 43 focused + 168 adjacent passes. |
| D6 | Profile and database pinned for every task and child process | Implemented | `task_environment`, enqueue-time capture, child inheritance, trigger lifetime capture, ContextVar worker environment and selected DB enforcement. `implemented-queue-profile-pinning.md`: 120 combined; `implemented-staged-process-lifecycle.md`: 62; independent combined process/profile check: 54. Direct interactive/API users must follow documented pinning contract. |
| O1 | Required immutable staged artifacts and explicit reviewed lead disposition | Implemented | Required files including empty candidates, process/outcome readiness, exact approved bundle hash, added/changed/missing rejection, explicit reviewed disposition; `test_orchestration_hardening.py`. Files are mutation-detected, not physically immutable. |
| O2 | Atomic idempotent staged import, including replay/error state | Implemented | `BEGIN IMMEDIATE` encloses eligibility, exact parsed approved bytes, writes, disposition and receipt; concurrent same-run imports and rollback fixtures. No universal deduplication promise for different runs. |
| O3 | Profile-scoped question-aware triage | Implemented | `test_triage_retains_distinct_questions_and_profiles`; queue triage is profile/question scoped, same-target name is insufficient to discard a lead. |
| O4 | Acquisition success, partial failure, and research completion are distinct | Implemented | Total/partial/successful collection fixtures preserve acquisition outcome and require review; collection never completes the research question. Source-specific partial outcomes are separately tested in USAspending and Maryland notices. |
| O5 | Worker liveness, deadlines, cancellation, and custom DB propagation | Implemented | Fixed deadline and heartbeat ownership, attempt tokens, transactional capacity reservation, terminal-state guards, actual process exit receipts and process-group cleanup; queue/staged lifecycle modules and independent real subprocess checks. |
| O6 | Trigger enqueue/receipt use one atomic connection | Implemented | Due trigger job and receipt use one transaction; simultaneous triggers, shared cooldown/hourly budget and receipt-failure rollback tests in `test_queue_lifecycle_hardening.py`. |
| O7 | Document supported execution paths and share task/lifecycle contracts | Implemented | `docs/EXECUTION_CONTRACT.md` documents supported staged, deterministic, and direct paths and their actual guarantees; ten CLI helps and exact example parsing checked. Say “shared finding writer and lifecycle/context contracts”; do not claim entity/lead/connection import implementations were all consolidated. Parent notified of stale `import --force` help. |
| S1 | Florida stable-ID import and foreign-key integrity | Implemented | `test_registry_ingest_integrity.py`: Florida refresh preserves parent ID, older child evidence, FK integrity and caller-owned rollback. |
| S2 | Idempotent undated registry officers/agents | Implemented | Same module: Ohio undated agent refresh/replay/concurrency is idempotent; dated/ended/filing-linked history and legacy duplicates are retained. Historical ambiguous duplicate repair remains evidence work. |
| S3 | USAspending failures/partials/page limits persist typed outcomes | Implemented | `implemented-usaspending.md`: 61 tests for every handler's empty/failure paths, retained partial rows, typed artifacts, 50-page cap and resumable continuation. JSON list responses intentionally now live under `results`; document consumer compatibility. |
| S4 | FEC search keys preserve full query scope | Implemented | FEC scope tests distinguish operation/query/cycle/filters/limit and exclude credentials/cursors from stable search identity. Shared canonical key used by reuse boundary. |
| S5 | Reuse shared environment/output/log contracts in legacy wrappers | Implemented | FEC/Ohio use shared environment loader; EDGAR/FARA/FEC/LittleSis use `search_log_util`; USAspending gets explicit output/outcome contract. `source-workflow-verification.txt`: 103 focused passes. Do not imply every copied wrapper in the source tree was migrated. |
| S6 | Migrate a representative source family to direct Python APIs | Implemented | Lincoln WFS `QueryOptions`/Python execution consumed directly by property router and monitor; tests assert typed object at internal boundary. Endpoint/source-specific behavior remains in its adapter. |
| W1 | Preserve network claim identity, verification, direction, and profile | Implemented | Network tests preserve separate same-endpoint claim IDs, each claim's own status, evidence, description, direction and profile; global structural links remain research-only. |
| W2 | Shared explicit public evidence policy for graph and dossiers | Implemented | Canonical `validate_connection_publication` shared across public graph/dossier and curator second hop; tests reject missing edge/upstream provenance and cross-profile leakage. `implemented-publication-integration.md`: 30 focused passes, later snapshot/release suite 63. |
| W3 | Visualization-only updates preserve arbitrary authored curation | Implemented | Curation regression preserves open-ended authored fields for visualization-only regeneration; no fixed allowlist that drops arbitrary prose. |
| W4 | Authoritative versioned publication snapshot propagates retractions | Implemented | Versioned hashes/finding fingerprints reject conflicting copies, retracted/nonverified/malformed claims, changed prose and same-timestamp live-DB corrections. TypeScript consumes authoritative selected snapshot. Retractions require refreshed reviewed content as described above. |
| W5 | One staged export/build entry point with valid CLI/cwd/error propagation | Implemented | `build_all` exports only to a fresh candidate directory, sets explicit content/public/DB paths, uses valid curator CLI and repo cwd, fails at first failed child and retains failed candidate. Staging/CLI tests plus synthetic selected-content browser parity; existing authored tree remains untouched. |
| W6 | Local and CI release validate the same artifact | Implemented | Local deployment and CI consume the artifact validated by `validate_release.py`; SHA receipt verification catches changed/missing/extra files and symlinks. Actual publish waits for valid source snapshot/receipts; synthetic artifact tests establish mechanics without claiming a current production release passed. |
| W7 | Enforced Astro/TypeScript checks and JSON boundary contracts | Implemented | Enforced `npm run check` and JSON validation; `astro-updated.txt`: 0 errors. Frontend contract log includes 621 citation cases, snapshots, changed-content and support-span checks plus invalid JSON/retry checks. Parent should retain final current-source rerun evidence. |
| W8 | Remove unused duplicate visual components | Implemented | Duplicate unused EgoNetworkV2 removed; CorporateStructureV2/demo moved to excluded experiments with README; current build 10,613 pages, no production demo route. |
| W9 | Canonical schema-aware article metadata/route loading | Implemented | Python `article_metadata.py` is the single schema-aware loader; web adapter calls its JSON boundary and hashes source bytes. Filename-route/YAML/index/backlink parity and invalid metadata tests plus selected-content web fixture. |
| W10 | Recoverable first-load search and correct readiness/focus behavior | Implemented | Shared retryable search initialization and React readiness state; Chromium tests cover typing before data, failed fetch then retry, focus trap/restore. `final-browser-tests.txt`. |
| W11 | Lazy visualization loading and safe tooltip text | Implemented | Visualization modules load only when relevant markers exist; tooltip content uses DOM text nodes. Browser injection test plus production build and built-citation checks. |
| Q1 | Deterministic CI executes existing pytest and unittest cases | Complete | 6,331 passed, 104 skipped, 164 deselected across four isolated non-overlapping partitions. Coverage includes every selected node; the actual critical-node collection guard passes. Result: reports/platform-hardening-2026-09-05/validation/verification-result.json. |
| Q2 | Publication checks fail closed on reviewed artifact; deploy depends on validation | Implemented | No advisory correctness checks or independent deploy race: deploy `needs: validate`, transfers immutable artifact, verifies it before upload, never rebuilds. Separate Python/web PR checks remain available despite content gate debt. |
| Q3 | Fix financial export default and explicit CLI override | Implemented | `test_financial_quality_export_path.py` checks actual canonical default and explicit override through exporter/gate CLI, then modifies amount to force MATH004 failure. Shared flow aggregation avoids mismatched exporter/checker arithmetic. |
| Q4 | Declare missing CLI dependencies and startup smoke coverage | Implemented | Locked Python 3.11 dependencies include cloudscraper, PyMuPDF and pypdf; direct web Playwright and check dependencies declared. Startup coverage and 12 isolated browser-runtime tests pass; both actual CA/NV runtime checks succeed without browser launch. |
| F1 | Semantic review required for exact dossier content version | Implemented | `implemented-workflows.md`: 39 tests including unrelated citation's automated PASS without semantic PASS, stale hash/failed/malformed/unbound/duplicate receipts and DB-free validation. Both skills require real review of current content. |
| F2 | Search reuse checks freshness, scope, acquisition outcome, result availability | Implemented | Search reuse requires exact scope, complete success, explicit freshness/version and an intact recoverable artifact; old log entries, partial/failed results, loss/tampering are not hits. Shared contract and reuse tests. |
| F3 | Timeline skill uses audited normalized-date API | Implemented | Both timeline skill mirrors use tracker `correct`; executable command fixtures preserve normalized date and audit contracts. |
| F4 | Analysis skill examples obey required argument/evidence contracts | Implemented | Analysis examples supply required source/evidence/quote arguments; six command parse/payload checks. Full skill validator: 0 errors, 16 existing add-registry warnings. Do not claim all legacy skill prose was rewritten. |
| F5 | Separate confidence ceilings from proposition probabilities | Implemented | Methodology and reviewed skills distinguish confidence ceilings, authenticity/allegation/event truth, and proposition probability. Hunch examples retain alternatives and disconfirmation. |
| F6 | Neutral hypotheses, applicability matrix, shared workflow references | Implemented | Canonical workflow contract owns applicability, pinned context, reuse and handoff. Nine paired skill updates use jurisdiction/question relevance, neutral alternatives and explicit unavailable/partial/not-applicable outcomes. |
| R1 | Preserve and inventory pre-existing uncommitted work | Complete | Ignored before-implementation archive/manifest preserves 4,867 files with hashes; tracked baseline recovery report enumerates restored inherited content and labels it unvalidated recovery. |
| R2 | Clean root clutter and exclude disposable/raw acquisition outputs | Complete | Tracked move manifest for 54 relocated artifacts and retention manifest for 2,370 local raw/generated artifacts (851,027,893 bytes); references updated and hashes preserved. No evidence deletion or blanket source ignore. |
| R3 | Branch-per-workstream and focused-commit policy in agent instructions | Complete | Branch-per-workstream policy, root agent pointers and focused commit examples are committed; implementation uses codex/platform-hardening-20260905. |
| R4 | Enforce staged artifact, credential, size, branch, and commit-message checks | Complete | Staged-blob artifact/credential/size/branch guard, exact-hash large-file exceptions, commit message policy, hooks and independent CI check; 26 temporary-repository tests. Existing Git LFS hooks preserved. |
| R5 | Separate recovered pre-existing work from validated implementation commits | Complete | Explicit recovery commits precede focused tested implementation commits; source-link repair is separate. All task-owned changes are accounted for. Concurrent research remains untouched. |
| R6 | Final requirement audit and remaining publication/data debt made explicit | Complete | Independent 46-row audit and final boundary review retained; final Python/frontend/build/artifact checks recorded in the hardening report. Actual publication remains blocked by documented evidence/review debt; no deploy artifact was created. |

Existing publication review debt will not be relabeled PASS. Code validation and tests can succeed while actual publication correctly remains blocked until content has a valid semantic review. Historical identity/orphan repairs require recoverable evidence; code fixes must not guess new research facts. Deterministic confidence/status violations can be audited and corrected with provenance after the shared writer is verified.

Durable implementation reports and validation evidence: [hardening report](../reports/platform-hardening-2026-09-05/README.md), [independent requirement audit](../reports/platform-hardening-2026-09-05/evidence/completion-audit.md). Temporary original logs live under `/tmp/osint-CUTDyZF1`; the durable report is the closeout record.
