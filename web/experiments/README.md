# Visualization prototypes

These historical design examples are retained for reference and are outside the
Astro route tree. Their sample research data is not publication evidence. The
supported components live in `src/components`; changes to a prototype do not
change the published site. The byte-identical unused `EgoNetworkV2` was removed;
use `src/components/EgoNetwork.tsx`.
